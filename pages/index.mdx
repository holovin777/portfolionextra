import {useSSG} from 'nextra/ssg';

export const getStaticProps = ({params}) => {
    return fetch(`http://localhost:8080/api/v1/customer/e28e100f-b2dd-49c7-b44a-81fb39cc249e`)
        .then(res => res.json())
        .then(customer => ({
            props: {
                ssg: {
                    customer: customer
                }
            },
            revalidate: 60
        }))
}

export const ContactIformation = () => {
    const {customer} = useSSG()
    return (
        <div>
            <div>
                <h3>{customer.firstName} {customer.lastName}</h3>
            </div>
            <div>
                {customer.phoneNumber}

            </div>
            <div>
                {customer.email}
            </div>
            <div>
                <a href={customer.website}>{customer.website}</a>
            </div>
            <div>
                {customer.residence}
            </div>
        </div>
    )
}

# Contact information

<ContactIformation />