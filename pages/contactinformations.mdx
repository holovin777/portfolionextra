import {useSSG} from 'nextra/ssg';

export const getStaticProps = ({params}) => {
    return fetch(`http://localhost:8080/api/v1/customer/4d936c7c-05ea-45f2-a91f-08518491c986`)
        .then(res => res.json())
        .then(customer => ({
            props: {
                ssg: {
                    customer: customer
                }
            },
            revalidate: 60
        }))
}

export const ContactInformations = () => {
    const {customer} = useSSG()
    return (
        <div>
            <div>
                <h3>{customer.firstName} {customer.lastName}</h3>
            </div>
            <div>
                {customer.phoneNumber}
            </div>
            <div>
                {customer.email}
            </div>
            {customer.website !== null &&
                <div>
                    <a href={customer.website}>{customer.website}</a>
                </div>
            }
            <div>
                {customer.residence}
            </div>
        </div>
    )
}

# Contact information's

<ContactInformations />
